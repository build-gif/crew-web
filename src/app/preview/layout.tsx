export const metadata = {
  robots: {
    index: false,
    follow: false,
  },
  title: "Crew of Builders — Preview",
};

export default function PreviewLayout({ children }: { children: React.ReactNode }) {
  return children;
}
